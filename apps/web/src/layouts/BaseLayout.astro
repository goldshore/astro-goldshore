---
import '../styles/global.css';
const { title = "GoldShore Labs" } = Astro.props;
---
<!DOCTYPE html>
<html lang="en" data-theme="dark">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" type="image/png" href="/favicon.png">
    <title>{title}</title>
</head>
<body class="min-h-screen flex flex-col">
    <header class="w-full sticky top-0 z-10 p-4 border-b bg-[var(--gs-surface)] border-[var(--gs-border)]">
        <div class="max-w-7xl mx-auto flex items-center justify-between flex-wrap">
            <a href="/" class="flex items-center gap-3 text-2xl gs-heading text-[var(--gs-brand-gold)] decoration-none">
                <img src="/logo.png" alt="GoldShore Labs Logo" class="h-8 w-auto" />
                <span>GoldShore</span>
            </a>

            <button id="mobile-menu-btn" class="md:hidden text-[var(--gs-text-primary)]">
                <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16m-7 6h7" />
                </svg>
            </button>

            <nav id="nav-menu" class="hidden md:flex flex-col md:flex-row w-full md:w-auto mt-4 md:mt-0 space-y-2 md:space-y-0 md:space-x-6 text-sm items-center">
                <a href="/about" class="text-[var(--gs-text-secondary)] hover:text-[var(--gs-text-primary)]">About</a>
                <a href="/app/dashboard" class="text-[var(--gs-text-secondary)] hover:text-[var(--gs-text-primary)]">Portal</a>
                <a href="/admin/overview" class="text-[var(--gs-text-secondary)] hover:text-[var(--gs-text-primary)]">Admin</a>
            </nav>
        </div>
    </header>
    <main class="flex-grow">
        <slot />
    </main>
    <footer class="w-full mt-16 p-6 border-t bg-[var(--gs-surface)] border-[var(--gs-border)]">
        <div class="max-w-7xl mx-auto flex flex-col items-center gap-4">
             <img src="/logo.png" alt="GoldShore Labs Logo" class="h-8 w-auto opacity-50" />
            <p class="text-center text-xs text-[var(--gs-text-secondary)]">© 2025 GoldShore Labs. Engineered with precision.</p>
        </div>
    </footer>

    <script>
        const btn = document.getElementById('mobile-menu-btn');
        const menu = document.getElementById('nav-menu');
        if (btn && menu) {
            btn.addEventListener('click', () => {
                menu.classList.toggle('hidden');
                menu.classList.toggle('flex');
            });
        }
    </script>
</body>
</html>
