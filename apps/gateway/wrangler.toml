name = "gs-gateway"
main = "src/index.ts"
compatibility_date = "2024-11-01"
node_compat = true

account_id = "YOUR_CLOUDFLARE_ACCOUNT_ID"
workers_dev = true

# Public gateway domain (later):
routes = [
  # { pattern = "gw.goldshore.ai/*", zone_name = "goldshore.ai" }
]

# Bindings to other Workers
[[services]]
binding = "API"
service = "gs-api"
environment = "production"

[ai]
binding = "AI"