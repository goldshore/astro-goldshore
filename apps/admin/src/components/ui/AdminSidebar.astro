<aside class="admin-sidebar">
  <div class="header">
    <h2>GoldShore <span class="badge">ADMIN</span></h2>
  </div>
  <nav>
    <a href="/">Dashboard</a>
    <div class="divider">System</div>
    <a href="/system/logs">Logs</a>
    <a href="/system/bindings">Bindings</a>
    <a href="/system/dns">DNS</a>
  </nav>
</aside>

<style>
  .admin-sidebar {
    width: 240px;
    background: #0b0b14;
    border-right: 1px solid rgba(255,255,255,0.08);
    display: flex;
    flex-direction: column;
    height: 100vh;
  }
  .header {
    padding: 1.5rem;
    border-bottom: 1px solid rgba(255,255,255,0.05);
  }
  h2 {
    margin: 0;
    font-size: 1rem;
    font-weight: 700;
    color: var(--gs-text);
    display: flex;
    align-items: center;
    gap: 0.5rem;
  }
  .badge {
    background: rgba(227, 179, 65, 0.2);
    color: var(--gs-gold);
    font-size: 0.6rem;
    padding: 0.1rem 0.3rem;
    border-radius: 4px;
  }
  nav {
    padding: 1rem;
    display: flex;
    flex-direction: column;
    gap: 0.2rem;
  }
  .divider {
    font-size: 0.7rem;
    text-transform: uppercase;
    color: var(--gs-text-muted);
    margin: 1rem 0 0.5rem 0.5rem;
    font-weight: 600;
  }
  nav a {
    display: block;
    padding: 0.5rem 0.8rem;
    border-radius: 6px;
    color: var(--gs-text-soft);
    text-decoration: none;
    font-size: 0.9rem;
    transition: all 0.2s;
  }
  nav a:hover {
    background: rgba(255, 255, 255, 0.07);
    color: var(--gs-text);
  }
</style>
