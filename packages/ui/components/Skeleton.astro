---
const { width = '100%', height = '1rem' } = Astro.props;
---
<div class="gs-skeleton" style={`width: ${width}; height: ${height};`}></div>
<style>
  .gs-skeleton {
    background: rgba(255,255,255,0.05);
    border-radius: 4px;
    animation: pulse 2s cubic-bezier(0.4, 0, 0.6, 1) infinite;
  }
  @keyframes pulse {
    0%, 100% { opacity: 1; }
    50% { opacity: .5; }
  }
</style>
